---
import { profile } from "../data/profile";

const asset = (path: string) => `${import.meta.env.BASE_URL.replace(/\/$/, "")}/${path}`;
---

<section class="relative overflow-hidden pb-16 pt-4 sm:pb-24 sm:pt-6">
  <div class="container">
    <header class="flex justify-end py-3 text-sm text-slate-200">
      <nav class="flex flex-wrap items-center gap-4 rounded-full border border-white/10 bg-slate-950/55 px-5 py-2 shadow-lg shadow-black/20 backdrop-blur-md" aria-label="Primary navigation">
        <a class="hover:text-cyan-300" href="#services">Services</a>
        <a class="hover:text-cyan-300" href="#projects">Projects</a>
        <a class="hover:text-cyan-300" href="#contact">Contact</a>
      </nav>
    </header>

    <div class="pt-4 lg:pt-6">
      <div class="section-frame overflow-hidden rounded-xl border border-cyan-300/20 bg-[#070b10] shadow-2xl shadow-cyan-950/30">
        <div class="relative overflow-hidden px-5 py-8 sm:px-8 lg:px-14 lg:py-12">
          <div class="absolute inset-0 bg-[radial-gradient(circle_at_52%_32%,rgba(34,211,238,0.16),transparent_30%),linear-gradient(90deg,rgba(8,13,22,0.98),rgba(8,13,22,0.78)_50%,rgba(8,13,22,0.98))]"></div>
          <div class="relative z-10 grid gap-8 lg:grid-cols-[minmax(0,1fr)_minmax(260px,320px)] lg:items-center xl:grid-cols-[minmax(0,1fr)_320px_minmax(0,1fr)]">
            <div>
              <p class="eyebrow">Available for practical project work</p>
              <h1 class="max-w-xl text-5xl font-black leading-[0.95] tracking-normal text-white sm:text-6xl xl:text-7xl">
                {profile.name}
              </h1>
              <p class="mt-5 max-w-md text-2xl font-semibold leading-tight text-slate-100">
                Python automation & web developer
              </p>
              <p class="mt-4 max-w-md text-base leading-7 text-slate-300">
                Websites, Python tools, and automation for real business problems.
              </p>
              <div class="mt-8 flex flex-wrap gap-3">
                <a class="button button-primary" href={profile.links.email} aria-label="Start a project with Nemanja Filipović">Start a project</a>
                <a class="button button-secondary" href={profile.links.github} target="_blank" rel="noreferrer">View GitHub</a>
                <a class="button button-secondary" href={profile.links.linkedin} target="_blank" rel="noreferrer">LinkedIn</a>
              </div>
              <div class="mt-6 flex flex-wrap gap-2 text-sm text-slate-400">
                {profile.meta.map((item) => <span class="chip">{item}</span>)}
              </div>
              <div class="mt-6 grid gap-3 rounded-xl border border-white/12 bg-slate-950/76 p-4 shadow-xl backdrop-blur">
                <p class="font-mono text-xs uppercase text-cyan-300">tools</p>
                <div class="flex flex-wrap gap-2">
                  <span class="chip">python</span>
                  <span class="chip">scraping</span>
                  <span class="chip">troubleshooting</span>
                </div>
              </div>
            </div>

            <figure class="relative mx-auto w-full max-w-[320px] overflow-hidden rounded-[28px] border border-cyan-300/24 bg-slate-950 shadow-2xl shadow-cyan-950/30 xl:max-w-[340px]">
              <div class="pointer-events-none absolute inset-0 z-20 bg-[linear-gradient(180deg,rgba(2,6,23,0)_52%,rgba(2,6,23,0.62)_100%)]"></div>
              <img
                class="relative z-10 aspect-[4/5] h-full w-full object-cover object-[50%_20%]"
                src={asset("images/nemanja-caricature.png")}
                alt="Illustrated portrait of Nemanja Filipović"
                width="1366"
                height="1536"
                loading="eager"
                decoding="async"
                fetchpriority="high"
              />
            </figure>

            <div class="max-w-none lg:col-span-2 xl:col-span-1 xl:ml-auto xl:max-w-sm">
              <p class="text-2xl font-bold leading-tight text-white sm:text-3xl lg:max-w-3xl xl:max-w-none">
                <span class="text-cyan-300">Available for freelance/project work</span> across business websites, Python tools, and data workflows.
              </p>
              <p class="mt-5 text-base leading-7 text-slate-300 lg:max-w-3xl xl:max-w-none">
                {profile.intro}
              </p>
              <p class="mt-4 text-sm leading-6 text-slate-400 lg:max-w-3xl xl:max-w-none">
                {profile.credibility}
              </p>
            </div>
          </div>
        </div>

        <div class="grid grid-cols-2 gap-2 bg-blue-500 px-5 py-4 text-center text-sm font-black text-white sm:flex sm:flex-wrap sm:items-center sm:justify-center sm:gap-x-6 sm:gap-y-3 lg:flex-nowrap lg:gap-x-5 lg:text-[0.82rem] xl:gap-x-8 xl:text-sm">
          {profile.proof.map((item) => <span class="tracking-normal">{item}</span>)}
        </div>
      </div>
    </div>
  </div>
</section>
